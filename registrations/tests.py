from django.test import TestCase
from utility import *
from datetime import *


hora = time(9,10,0)
delta = timedelta(hours=9, minutes=1)

print (hora.minute)
print (hora.hour)

"""
        for horario in free:
            horarioTD = timedelta(hours=int(horario.split(":")[0]),minutes=int(horario.split(":")[1])
            for j in self,.busy:
                if j.append:
                    appbeginTD = timedelta(hours=j.appbegin.hour, minutes= j.appbegin.minute)
                    appendTD = timedelta(hours=j.append.hour, minutes= j.append.minute)
               # if horario <= appendTD and :

"""